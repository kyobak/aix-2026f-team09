# AI 협업 기록 / AI Collaboration Log

작성 원칙: 프롬프트 나열이 아니라 **판단 근거**를 남긴다.
Principle: record your **reasoning**, not just prompts.

---

## [이슈 #__] 제목 / Title

**목표(스펙) / Spec**
- 입력 Input:
- 처리 Processing:
- 출력 Output:
- 실패 조건 Failure:

**요청한 프롬프트 요지 / Prompt (summary)**

**결과에 대한 판단 / Decisions**
- 채택한 부분과 이유 / Accepted, because:
- 수정한 부분과 이유 / Changed, because:
- 폐기한 부분과 이유 / Rejected, because:

**검증 방법 / How it was verified**

---
(이슈 단위로 반복 / repeat per issue)

## 2026-09-11 · 메모 검색 기능 (2주차 활동)

**지시**

메모 검색 기능 만들어줘. 제목이랑 본문에서 키워드로 찾을 수 있게.

**채택 여부**

미채택

에이전트가 기존 `memo-seed` 프로젝트에 검색 API를 추가하지 않고, 별도의 브라우저용 메모 앱을 새로 만들었다. 따라서 기존 `service.js`와 `routes.js`에 적용할 수 없었으며, 요구된 `GET /memos/search?q` 엔드포인트도 구현되지 않았다.

**참고**

기존 코드, 데이터베이스 스키마, 프로젝트 규약과 종료조건을 제공하지 않아 에이전트가 요청을 독립적인 메모 앱 제작으로 해석했다. 그 결과 제목과 본문을 검색하는 기능 자체는 작동했지만, 빈 검색어 검증, 규정된 API 응답 형식, `user_id`를 이용한 본인 메모 제한 조건은 반영되지 않았다.

## 2026-09-11 · 메모 검색 기능 (2주차 활동)

**목표(스펙) / Spec**
- 입력 Input: 검색 키워드 `q` (쿼리스트링), 요청자 `user_id`
- 처리 Processing: 본인 소유 메모 중 **제목 또는 본문**에 키워드가 포함된 것을 찾아 최신순 정렬
- 출력 Output: `{ ok: true, data: [{ id, title, created_at }] }`
- 실패 조건 Failure: `q`가 비었거나 공백뿐이면 `400` / `{ ok: false, error: "SEARCH_QUERY_REQUIRED" }`

**요청한 프롬프트 요지 / Prompt (summary)**

네 블록으로 나눠 제시했다.
`[지시]` 한 줄 · `[규약]` CONVENTIONS.md 전문 · `[근거]` schema.sql·service.js·routes.js 전문 ·
`[종료조건]` 네 항목(호출 형태 / 매칭 범위 / 본인 메모 한정 / 빈 입력 처리).

원문은 요약 없이 `memo-seed/B조-프롬프트-원문.txt` (146줄)에 보관했다.

**결과에 대한 판단 / Decisions**

- 채택한 부분과 이유 / Accepted, because:
  `service.js`의 `searchMemos`와 `routes.js`의 `GET /memos/search`를 그대로 받았다.  규약 네 항목(계층 분리 · 응답 형식 · 명명 규칙 · `user_id` 조건)을 모두 지켰고,  존재하지 않는 컬럼이나 함수를 지어낸 곳이 없었다.  특히 **`/memos/search`를 `/memos/:id`보다 위에 배치한 점**을 그대로 받아들였다.  Express는 등록 순서대로 먼저 일치하는 라우트가 이기므로, 아래에 두면 `search`가`:id` 값으로 잡혀 검색이 항상 `404 MEMO_NOT_FOUND`가 된다.  `[근거]`에 `routes.js` 전문을 넣었기 때문에 가능했던 판단이라고 본다.

- 수정한 부분과 이유 / Changed, because:
  `LIKE` 패턴에 `ESCAPE` 처리를 추가했다. 종료조건에는 없던 항목이다.  키워드에 `%`나 `_`가 들어오면 SQL 와일드카드로 해석돼 의도와 달리 전체가 매칭된다. `%`·`_`·`\`를 문자 그대로 다루도록 바꿨다.  종료조건 밖의 판단이므로 여기에 근거를 남긴다.

- 폐기한 부분과 이유 / Rejected, because:
  없음.

  **검증 방법 / How it was verified**

예시 데이터(사용자 2명, 메모 8건)를 넣고 7개 시나리오를 실행해 확인했다.

| 확인한 것 | 결과 |
| :-- | :-- |
| 제목 매칭 — `q=라이선스` (user 1) | `라이선스 조사 메모` 1건 |
| **본문** 매칭 — `q=404` (user 1) | `라우트 순서 주의` 1건 (제목엔 없는 단어) |
| 권한 — `q=라이선스` (user 2) | 1번 사용자 메모 **0건**, 본인 것만 1건 |
| 빈 입력 — `q=` | `HTTP 400` / `SEARCH_QUERY_REQUIRED` |
| ESCAPE — `q=%` | 전체 7건이 아니라 `%`가 실제로 든 1건 |
| 라우트 순서 — `GET /memos/3` | `HTTP 200` (단건 조회가 깨지지 않음) |
| 전체 목록 — `GET /memos` | 7건, 최신순 |


## 2026-09-18 · 3주차 활동지 후보 B 문제 정의

### 목표(스펙) / Spec
- **입력:** 3주차 활동지 양식(사용자/상황/페인포인트/성공 기준 네 칸), 팀이 이전에 정리해둔
  캠퍼스 대기 관련 아이디어 메모, "해결책은 쓰지 않는다"는 활동지 규칙
- **처리:** 아이디어를 네 요소로 분해하고, 각 칸을 관찰 가능한 수준까지 구체화
- **출력:** 표 한 칸씩 채운 초안 + "[사용자]는 [상황]에서 [페인포인트] 때문에 [손해]를 본다"
  형식의 한 문장
- **실패 조건:** ① 사용자가 "ERICA 학생"처럼 범위만 넓고 특정되지 않으면 실패
  ② 페인포인트에 빈도나 크기가 없으면 실패 ③ 기능·기술이 한 글자라도 들어가면 실패
  ④ 성공 기준이 "편해진다"처럼 확인 불가능한 말이면 실패

### 요청한 프롬프트 요지 / Prompt
(원문 그대로 붙여넣기)

### 결과에 대한 판단 / Decisions
- **채택:** 사용자를 "창의인재원 거주 + 평일 학식 이용 + 연강 사이 1시간 이하"로 좁힌 부분.
  이 조건이 그대로 인터뷰 섭외 기준이 되어 대상을 고르는 시간이 줄었다.
- **수정:** 초안의 페인포인트는 "불편하다"에 그쳐서, 우리가 실제로 세어볼 수 있는
  헛걸음 횟수와 줄 포기 횟수로 바꿨다. 관찰로 참/거짓을 가릴 수 없는 문장은
  3절의 가정 표에 올릴 수 없기 때문이다.
- **폐기:** 후보 B 안에 주차장·오픈스페이스까지 묶어 다섯 장소를 한 칸에 넣은 구성.
  장소가 늘면 사용자와 상황이 흐려져 네 칸이 다시 막연해진다. 학식과 세탁실 두 곳으로 줄였다.

### 검증 방법 / How it was verified
완성된 한 문장을 팀원 앞에서 소리 내어 읽고 어색한 칸을 찾았다. 네 칸 각각에 대해
"이걸 어떻게 확인할 것인가"를 물어 답이 안 나오는 칸은 다시 썼다.
그 결과가 3절 가정 표 1~4번이다.

---

## 2026-09-18 · 인터뷰 질문 3개 작성

### 목표(스펙) / Spec
- **입력:** 확정한 후보 B 문제 정의, 활동지의 "과거형으로 물어라" 규칙,
  질문 3개라는 개수 제한
- **처리:** 가정 2번(세탁실 헛걸음)과 3번(알면 행동을 바꾸는가)을 확인할 질문으로 변환
- **출력:** 인터뷰에서 그대로 읽을 수 있는 질문 3개
- **실패 조건:** ① "쓰시겠어요?", "있으면 좋겠죠?" 같은 미래 의향·가정형이면 실패
  ② 답이 예/아니오로 끝나 경험을 못 끌어내면 실패 ③ 우리 아이디어를 먼저 설명해야
  이해되는 질문이면 실패(답을 유도하게 된다)

### 요청한 프롬프트 요지 / Prompt
(원문 그대로 붙여넣기)

### 결과에 대한 판단 / Decisions
- **채택:** "최근에 줄을 보고 돌아선 적 있으세요? 그때 대신 어떻게 하셨어요?"
  — 과거 경험과 현재의 대안 행동을 한 번에 물어서, 활동지의 '지금은 어떻게 하고 있는가'
  칸까지 이 질문 하나로 채워진다.
- **수정:** "세탁기 상황을 알 수 있으면 편하시겠어요?"를
  "지난주에 세탁실에 몇 번 내려가셨어요? 처음에 바로 비어 있던 적은요?"로 바꿨다.
  전자는 거의 모두 "네"가 나와서 아무것도 거르지 못한다.
- **폐기:** 알림 주기, 선호 화면 같은 기능 선호를 묻는 질문 2개.
  기능은 4~5주차에 정하므로 지금 물으면 답이 먼저 우리를 구속한다.

### 검증 방법 / How it was verified
질문마다 "이 답이 아니오로 나오면 후보 B를 접을 수 있는가"를 확인했다.
아니오가 나와도 아무 일도 일어나지 않는 질문은 뺐다. 팀원끼리 한 번 예행해
답이 한 문장으로 끝나버리는 질문을 고쳤다.

---

## 2026-09-18 · 해결책 중심 서술을 활동지에서 제외한 판단

### 목표(스펙) / Spec
- **입력:** 팀이 이전에 정리한 준비계획서(측정 방식, 기술 스택, 일정 포함),
  3주차 활동지 양식
- **처리:** 준비계획서 내용 중 3주차 활동지에 들어갈 수 있는 부분만 추리기
- **출력:** 활동지 1~3절에 들어갈 문장
- **실패 조건:** 문제 칸에 "어떻게 만들 것인가"가 섞여 들어오면 실패

### 요청한 프롬프트 요지 / Prompt
구상한 주제를 AI라면 어떤 식으로 후보군을 나누고 인터뷰 할 것인지 파악을 위한 프롬프트 제시

### 결과에 대한 판단 / Decisions
- **채택:** 이해관계자 정리(배식 담당 직원, 앱을 쓰지 않는 세탁실 이용자)와
  관찰 항목·시간대. 둘 다 문제 쪽 서술이라 그대로 2·3절에 들어갔다.
- **수정:** "카메라로 줄 인원과 처리 속도를 센다"를 관찰 계획의 '무엇을 셀 것인가'로 옮겼다.
  같은 숫자라도 이번 주에는 사람이 손으로 세는 활동이고, 기계가 세는 것은 뒤 문제다.
- **폐기:** 영상 인원 계수, 대기시간 추정식, PWA·DB 등 기술 스택, 주차 구성 전부.
  활동지가 "무엇을 만들지는 쓰지 않는다"고 못 박았고, 해결책을 먼저 적으면 이미 정한 답에
  맞춰 사용자를 끼워 맞추게 된다. 이건 활동지가 경고한 '가짜 사용자형'과 같은 실패다.

### 검증 방법 / How it was verified
완성한 활동지에서 기능·도구·기술 이름을 전부 찾아 지운 뒤에도 네 칸이 성립하는지 확인했다.
성립했으므로 문제 정의가 해결책에 기대고 있지 않다고 판단했다.

---

**그 외 AI 사용 (산출물에 들어가지 않음):** 용어 확인, 표 문장 다듬기, 번역 목적으로
간헐적으로 참고했으며 결과물에 반영된 것은 없다.

---

## 2026-09-23 · 요구사항 명세 ③ Acceptance Criteria 작성

###목표(스펙) / Spec
- **입력:** 명세서 ③절 AC 양식(정상 경로 2개 + 실패 경로 1개 이상, 판정 방법 필수), 팀 프로젝트 학습자료(대기시간 공식, 세탁기 상태 규칙, 가상 대기열, 제보 제한 등 요구사항 내용)
- **처리:** 요구사항 중 이용자가 체감하는 동작을 골라 EARS 문형(WHEN/IF … THE … SHALL)으로 바꾸고, 각 문장마다 참/거짓을 가릴 수 있는 판정 방법을 붙임
- **출력:** AC-1 ~ AC-6 (정상 3개, 실패 3개) 표
- **실패 조건:** ① 판정 방법이 비어 있으면 실패 ② 판정 방법이 구현을 전제하면(코드, API, 테스트 이름) 실패 ③ 문장에 숫자 기준(시간, 횟수)이 없어 판정이 사람마다 달라지면 실패 ④ 실패 경로가 "오류가 난다" 수준에 그치고 시스템이 대신 무엇을 하는지 없으면 실패

### 요청한 프롬프트 요지 / Prompt

두 단계로 나눠 제시했다.
1차: [지시] AC-6까지 작성 · [근거] 학습자료 PDF, AC 양식 원문 · [종료조건] 양식이 정한 최소 경로 수와 판정 방법 필수
2차: 1차 결과를 검토한 뒤 "명세는 구현 전에 쓰는 것이므로 판정 방법은 코드가 아니라 실제 검증 행동으로 채울 것"을 조건으로 추가해 재요청

### 결과에 대한 판단 / Decisions
- **채택:** 6개 행의 동작 선정과 정상/실패 배분. 대기시간 표시, 세탁기 사용 시작 판정, 빈 기기 호출을 정상 경로로, 영상 전송 차단, 호출 무응답 만료, 제보 반복 제한을 실패 경로로 나눈 구성이 우리 요구사항의 핵심 규칙과 겹쳤다. 모든 문장에 10초, 30초, 5분, 10분 같은 숫자 기준이 들어가 있어 판정이 사람마다 달라지지 않는다.
- **수정:** 1차 결과의 판정 방법이 요청 명령, 응답 코드, 테스트 함수 이름으로 채워져 있었다. 학습자료가 이미 구현된 코드를 설명하는 문서라서 AI가 구현을 근거로 검증을 거꾸로 쓴 것이다. 명세 단계에서는 코드가 없으므로 이 판정은 수행할 수 없다. 스톱워치로 시간을 재고, 팀원이 직접 줄을 서고, 손으로 인원을 세어 화면 값과 비교하는 현장 검증으로 바꾸게 했다. EARS 문장에서도 필드명과 상태 코드 같은 구현 용어를 빼고 이용자가 보는 결과("거부 안내", "다음 가능 시각 안내")로 바꿨다.
- **폐기:** 판정 방법의 코드·API·테스트 이름 전부. 또 AC-3(영상 미저장)은 현장 관찰만으로 "저장하지 않음"을 완전히 증명하기 어렵다는 한계가 있어, 발표 시 서버 저장소를 직접 열어 보여 주는 방식을 팀이 따로 정하기로 했다.

### 검증 방법 / How it was verified

각 행의 판정 방법에서 코드·기술 용어를 모두 찾아 지운 뒤에도 검증 절차가 성립하는지 확인했다. 코드를 모르는 팀원이 표만 읽고 스톱워치와 휴대폰만으로 수행할 수 있는지 행마다 따져 봤고, 실패 경로 3개는 "거부되었다" 또는 "넘어갔다"를 눈으로 확인할 수 있는 장면이 있는지 확인했다.

---

